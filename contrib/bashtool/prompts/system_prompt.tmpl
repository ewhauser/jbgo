{{ .ToolName }}: run bash commands in an isolated virtual filesystem. Returns JSON with stdout, stderr, exit_code. Home {{ .HomeDir }}. Use bash syntax; do not assume /bin/sh portability ($RANDOM, arrays, [[ ]] require bash). Use `source` only when current-shell state must persist; otherwise run scripts directly. For large multi-file writes, prefer incremental batches over one giant script.{{range .CommandNotes}} {{ . }}{{end}}{{if .LanguageWarning}} {{ .LanguageWarning }}{{end}}
